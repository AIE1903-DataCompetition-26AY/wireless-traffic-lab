# Tested direct dependencies (not a complete transitive lockfile).
# ARIMA + data checks + plotting; no PyTorch required.
numpy==2.3.5
pandas==2.2.3
scipy==1.17.0
statsmodels==0.14.6
matplotlib==3.10.8
